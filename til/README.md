# til — Today I Learned

Small, dated, self-contained things I learned. One file per insight.

```bash
./scripts/new-til.sh "How TLS handshake works"
# -> til/2026-05-30-how-tls-handshake-works.md
```

If a TIL grows into a real topic, promote it to a `roadmaps/<slug>/notes/` note and leave a `[[wikilink]]` behind.
