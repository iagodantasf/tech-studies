---
title: Design tokens
track: design-system
group: Design tokens
tags: [design-system]
---

# Design tokens

Lessons in this group, roughly in build order:

- [[defining-design-tokens]] — Design tokens are named, platform-agnostic key-value pairs (e.g
- [[file-formats]] — The file format is how tokens are stored and shipped — a structured source (usually JSON) that a build…
- [[design-editor]] — The design editor is the visual tool (Figma, Sketch, Penpot) where tokens are authored and applied as…
